# Token rules — nano (eco always-on, ~300 tokens)

Read footprint + brief + coding-rules(mini/nano) + AGENTS.md first, then stop.
Prefer grep/glob. Slices, not wholes. Never node_modules/.next/lockfiles/binaries.
Stay inside --scope unless blocked. Smallest diff. Batch calls. Terse summary.
Verify once at end: lint -> build -> unit. --plan: no writes/builds/tests, ~30-line plan.
--eco: skip e2e/visual unless layout changed, summary <15 lines.
