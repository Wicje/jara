---
name: core-web-vitals
description: "Vendor (on-demand): Optimize Core Web Vitals (LCP, INP, CLS) for better page experience using field and lab evidence. Use when asked to "improve Core Web Vitals", "fix LCP", "reduce CLS", "optimize INP", "page experience optimization", or "fix layout shifts". Use for: Core Web Vitals measurement and budgets (LCP, INP, CLS)."
---

# core-web-vitals (vendor skill, on-demand)

> Upstream install name: `core-web-vitals` (https://github.com/addyosmani/web-quality-skills.git, pinned afa8da9).
> Full reference is `FULL.md` in this folder — read it ONLY when the brief
> matches (Core Web Vitals measurement and budgets (LCP, INP, CLS)). Otherwise ignore this skill entirely.

1. State a one-line design read first (page kind + audience + vibe).
2. Read `FULL.md` only on a match; apply its dials/rules to the task.
3. Never inject `FULL.md` into always-loaded context.

Update: `./factory.sh skills update core-web-vitals && ./factory.sh skills sync <project>`.
