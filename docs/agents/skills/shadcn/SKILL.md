---
name: shadcn
description: "Vendor (on-demand): Manages shadcn components and projects: adding, searching, fixing, debugging, styling, composing UI. Triggers for shadcn/ui, registries, presets, components.json. Use for: working with shadcn/ui components, components.json, registries, presets."
---

# shadcn (vendor skill, on-demand)

> Upstream install name: `shadcn` (https://github.com/shadcn-ui/ui.git, pinned a87a63b).
> Full reference is `FULL.md` in this folder — read it ONLY when the brief
> matches (working with shadcn/ui components, components.json, registries, presets). Otherwise ignore this skill entirely.

1. State a one-line design read first (page kind + audience + vibe).
2. Read `FULL.md` only on a match; apply its dials/rules to the task.
3. Never inject `FULL.md` into always-loaded context.

Update: `./factory.sh skills update shadcn && ./factory.sh skills sync <project>`.
