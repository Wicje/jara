---
name: performance
description: "Vendor (on-demand): Optimize web performance for faster loading and better user experience. Use when asked to "speed up my site", "optimize performance", "reduce load time", "fix slow loading", "improve page speed", or "performance audit". Use for: frontend performance optimization."
---

# performance (vendor skill, on-demand)

> Upstream install name: `performance` (https://github.com/addyosmani/web-quality-skills.git, pinned afa8da9).
> Full reference is `FULL.md` in this folder — read it ONLY when the brief
> matches (frontend performance optimization). Otherwise ignore this skill entirely.

1. State a one-line design read first (page kind + audience + vibe).
2. Read `FULL.md` only on a match; apply its dials/rules to the task.
3. Never inject `FULL.md` into always-loaded context.

Update: `./factory.sh skills update performance && ./factory.sh skills sync <project>`.
