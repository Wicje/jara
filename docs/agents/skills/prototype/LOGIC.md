# LOGIC.md — logic prototype branch (on demand)

Single file, one question. No DB writes, no auth, no tests.
Run: one command (e.g. `npx tsx prototype.ts` or open `prototype.html`).
Capture: paste output (<=20 lines) into spec + footprint, then delete.
