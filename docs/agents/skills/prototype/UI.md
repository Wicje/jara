# UI.md — UI prototype branch (on demand)

One route, N variants via `?variant=`. Same copy/data across variants.
Compare via screenshots (`test:visual:update` locally, never in CI).
No backend, no persistence. Pick winner -> spec records tokens/classes only.
