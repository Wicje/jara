---
name: seo
description: "Vendor (on-demand): Optimize for search engine visibility and ranking. Use when asked to "improve SEO", "optimize for search", "fix meta tags", "add structured data", "sitemap optimization", or "search engine optimization". Use for: SEO work (metadata, structured data, crawlability)."
---

# seo (vendor skill, on-demand)

> Upstream install name: `seo` (https://github.com/addyosmani/web-quality-skills.git, pinned afa8da9).
> Full reference is `FULL.md` in this folder — read it ONLY when the brief
> matches (SEO work (metadata, structured data, crawlability)). Otherwise ignore this skill entirely.

1. State a one-line design read first (page kind + audience + vibe).
2. Read `FULL.md` only on a match; apply its dials/rules to the task.
3. Never inject `FULL.md` into always-loaded context.

Update: `./factory.sh skills update seo && ./factory.sh skills sync <project>`.
