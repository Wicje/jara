---
name: soft-skill
description: "Vendor (on-demand): Teaches the AI to design like a high-end agency. Defines the exact fonts, spacing, shadows, card structures, and animations that make a website feel expensive. Blocks all the common defaults that make AI designs look cheap or generic. Use for: high-end agency-grade visual design (fonts, spacing, shadows, motion)."
---

# soft-skill (vendor skill, on-demand)

> Upstream install name: `high-end-visual-design` (https://github.com/Leonxlnx/taste-skill.git, pinned 5217fb4).
> Full reference is `FULL.md` in this folder — read it ONLY when the brief
> matches (high-end agency-grade visual design (fonts, spacing, shadows, motion)). Otherwise ignore this skill entirely.

1. State a one-line design read first (page kind + audience + vibe).
2. Read `FULL.md` only on a match; apply its dials/rules to the task.
3. Never inject `FULL.md` into always-loaded context.

Update: `./factory.sh skills update soft-skill && ./factory.sh skills sync <project>`.
