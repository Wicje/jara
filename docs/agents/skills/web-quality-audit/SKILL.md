---
name: web-quality-audit
description: "Vendor (on-demand): Run an evidence-led web quality audit covering performance, accessibility, SEO, best practices, and agentic browsing. Use when asked to "audit my site", "review web quality", "run lighthouse audit", "check page quality", or "optimize my website". Use for: evidence-led site quality audits (performance, a11y, SEO, Lighthouse)."
---

# web-quality-audit (vendor skill, on-demand)

> Upstream install name: `web-quality-audit` (https://github.com/addyosmani/web-quality-skills.git, pinned afa8da9).
> Full reference is `FULL.md` in this folder — read it ONLY when the brief
> matches (evidence-led site quality audits (performance, a11y, SEO, Lighthouse)). Otherwise ignore this skill entirely.

1. State a one-line design read first (page kind + audience + vibe).
2. Read `FULL.md` only on a match; apply its dials/rules to the task.
3. Never inject `FULL.md` into always-loaded context.

Update: `./factory.sh skills update web-quality-audit && ./factory.sh skills sync <project>`.
