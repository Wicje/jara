# Ticket template — copy to .scratch/<slug>/issues/NN-<slug>.md

## Title

NN — short behavioral title (e.g. `01 — hero renders with CTA`)

## Goal

One paragraph: what demoable behavior this adds.

## Acceptance

- [ ] Observable outcome 1 (e.g. CTA visible at 375px, keyboard-focusable)
- [ ] Observable outcome 2 (e.g. `lint` + `build` + `unit` green)

## Seam under test

Public interface exercised (props / route / action). No file paths.

## Blocked by

`none` or `NN-<slug>` list. Work the frontier first.

## Notes

Anything the implementer must know. No code dumps.
